# URL of the notify-lead Edge Function (deployed to your Supabase project).
# The URL is public — it's called from the browser and shipped in the JS bundle.
PUBLIC_LEAD_ENDPOINT_URL=https://your-project-ref.supabase.co/functions/v1/notify-lead
